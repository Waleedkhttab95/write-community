# Base URL of the Cultural Initiative CMS that serves مجتمع الكتابة articles.
# Articles are fetched from ${CMS_API_URL}/api/articles?site=write-community at
# build time. Defaults to the production CMS if unset.
CMS_API_URL=https://cultural-managment.com
